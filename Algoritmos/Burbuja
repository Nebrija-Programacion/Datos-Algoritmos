#include <iostream>
#include <vector>
using namespace std;
vector<int> burbuja(vector<int>ala){
    int temp;
    int m=ala.size();
    for (int i=0;i<m;i++)
       for(int j=1;j<m-i;j++)
        if(ala.at(j-1)>ala.at(j)){
                swap(ala.at(j-1),ala.at(j));
            }


    for (int i=0;i<ala.size();i++) {
        cout<<ala.at(i);
    }}
int main(){
    vector<int>ala{5,4,1,6,9,7,3};
    burbuja(ala);
    return 0;
}
